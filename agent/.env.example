# Agent points at bi and home-report-ai over HTTP.
# Default values match local-host development.
# For RunPod / production, set these to actual service URLs.
BI_BASE=http://localhost:8000
HOME_REPORT_BASE=http://localhost:8001
